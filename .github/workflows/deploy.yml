name: Deploy

on:
  push:
    branches: [main]

jobs:
  deploy:
    uses: etozhealkhipce/senior-script-kiddie-k3s/.github/workflows/deploy-app.yml@main
    with:
      app_name: travel-tips-frontend
      app_host: travel-tips.sskd.tech
    secrets: inherit
